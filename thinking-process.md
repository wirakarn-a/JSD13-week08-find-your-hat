## Find Your Hat - JavaScript Assessment 🎩

### Thinking Process
1. เริ่มจากทำความเข้าใจโจทย์ และแพลนก่อนว่าต้องมีอะไรบ้าง
    - เป็นเกมเดินหาหมวกในแผนที่
    - ถ้าเจอหมวกชนะ ถ้าตกหลุมหรือเดินออกนอกแผนที่แพ้

2. ลิสต์สิ่งที่ต้องมีในงาน
    - class Actor ที่เดินได้ 4 ทิศทาง
    - map ที่เก็บตำแหน่งของ hole, hat, actor
    - random ตำแหน่งของแต่ละอย่างตอนเริ่มเกม
    - print map ออกมาให้เห็นตำแหน่งปัจจุบัน
    - game logic มีแพ้/ชนะทุกครั้งที่เดิน

3. import { createInterface } from "node:readline" เพื่อรับข้อความที่เราจะพิมพ์ลงใน terminal โดยเมื่อเรา run คำสั่ง node findyourhat.js terminal จะรอรับคำสั่งที่เราพิมพ์

4. วางแผนโครงสร้างการเขียน และการสร้าง map จากตัวอย่างงาน zoo

5. สร้าง class Actor ที่มี property x, y เพื่อเก็บตำแหน่งปัจจุบัน มี method moveRight(), moveLeft(), moveUp(), moveDown() จะมีปรับค่า x และ y ทีละ 1 ตามทิศที่เดิน แล้วเรียก checkGameStatus() ก่อนขยับทุกครั้ง

6. เขียนฟังก์ชัน displayMap() ตาม grid ทีละแถว ทีละคอลัมน์ เพื่อเช็คว่าตำแหน่งนั้นตรงกับ actor, hat, หรือ hole ไหน แล้ว print ออกมาเป็นตารางให้เห็นภาพรวมของสนามทั้งหมด

7. ใช้ Math.random() สุ่มค่า x, y ของ hat และ holes ก่อน แล้วเช็คว่าตำแหน่งที่สุ่มได้ไม่ซ้ำกับตำแหน่งอื่นที่มีอยู่แล้ว (ไม่ให้ hole ทับ hat หรือทับ actor) 

8. เขียน game loop ด้วย askForCommand() รับคำสั่ง w/a/s/d จากผู้เล่น แล้วส่งต่อให้ handleCommand() เรียก method การเดินที่ตรงกับปุ่มที่กด จากนั้นเรียก displayMap()ใหม่ทุกครั้งเพื่ออัพเดตแผนที่ให้ตรงกับตำแหน่งล่าสุด

9. ทุกครั้งที่ actor เดิน จะเรียก checkGameStatus() เพื่อเช็คตำแหน่งใหม่
    - ถ้าตรงกับ hole คือแพ้ 
    - ถ้าตำแหน่งเกินขอบเขตแผนที่คือแพ้ 
    - ถ้าตรงกับตำแหน่ง hat คือชนะ


10. เพิ่ม Logic ใน checkGameStatus()
    - ถ้าอยู่นอกขอบเขต = 🚫 You went out of bounds! Game over แล้วจบเกม
    - ถ้าเจอ O = 💀 You fell into a hole! Game over. แล้วจบเกม
    - ถ้าเจอ 🎩 = 🎉 You found the hat! You win! แล้วจบเกม
    - ถ้าเป็นพื้นที่ว่างให้เดินต่อได้

11. เพิ่มคำสั่ง q ให้ผู้เล่นออกจากเกมได้